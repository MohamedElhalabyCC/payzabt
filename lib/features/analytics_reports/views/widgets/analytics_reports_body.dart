import 'package:flutter/material.dart';

class AnalyticsReportsBody extends StatelessWidget {
  const AnalyticsReportsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: []);
  }
}